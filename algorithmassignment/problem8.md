```
Step1:Start the algorithm
Step2: accept the alphabet ch
Step3:  IF (ch == 'a' || ch == 'A' ||
            ch == 'e' || ch == 'E' ||
            ch == 'i' || ch == 'I' ||
            ch == 'o' || ch == 'O' ||
            ch == 'u' || ch == 'U' )

Print "Vowel"

ELSE

Print "Not vowel"

Step4: End the algorithm