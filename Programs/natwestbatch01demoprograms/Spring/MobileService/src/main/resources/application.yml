spring:
  application:
    name: mobile-service
  data:
    mongodb:
      database: mobileDB
      port: 27017
      host: localhost
server:
  port: 8082

  
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true